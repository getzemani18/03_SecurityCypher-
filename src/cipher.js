window.cipher = { //Espero que funcione, que codifique y decodifique 
      encode:(offset, string) => { //Funcion para empezar a codificar, esperamos que con ofsset se mueva con el numero que le indiquemos, ademas con el string para String es lo ingresan
           let cifrar= ""; //Nos traemos el id del index.html y index.js...
           for(let i=0; i< string.length; i++) {
if(string[i].charCodeAt() >= 65 && string[i].charCodeAt() <= 90)

           }// Itera cada que se ingresa dentro del string y la funcion
           console.log(string)
      }
}